# Docker Node.js MongoDB NextJs React

Setup Node.js app using Docker

# Quick Start

`git clone https://github.com/jurikos/docker-nodejs-mongodb-nextjs-react.git`

`cd docker-nodejs-mongodb-nextjs-react`

`docker-compose up`
